# 安装指南 · INSTALL

本工作台是**单文件 HTML**，有以下 4 种安装方式，按你的使用场景选一种即可。

---

## 方式一：零安装 —— 浏览器直接打开（最快，推荐先用这个）

**电脑：**
1. 下载本仓库的 `workspace.html`
2. 双击文件，用 Chrome / Edge 浏览器打开
3. 页面已预置示例数据（带【示例】标记），熟悉功能后点右上角「清空示例」

**手机：**
1. 把 `workspace.html` 通过微信/网盘传到手机，用手机浏览器打开
2. iOS Safari：分享 → **添加到主屏幕**，即可像 App 一样使用
3. 安卓 Chrome：菜单 → **添加到主屏幕**

**数据说明（重要）：**
- 数据保存在**当前浏览器**的 localStorage 里，清浏览器缓存前请先「导出备份」
- 换电脑 / 换浏览器 = 先在旧设备「导出备份」（JSON 文件），再到新设备「导入恢复」
- 本地模式没有跨设备同步，如需多端同步请用方式二

---

## 方式二：WorkBuddy 安装（在线存储 + 多端同步，长期使用推荐）

前提：你在使用 WorkBuddy（已内置「资料库」数据表能力）。

1. 打开 WorkBuddy，新建对话
2. 打开 [`prompts/workbuddy-install-prompt.md`](prompts/workbuddy-install-prompt.md)，**全选复制全部内容**
3. 粘贴发给 AI，一句话都不用改
4. AI 会自动完成：
   - 创建 8 张数据表（任务 / 随手记录 / 素材库 / 选题看板 / 账号驾驶舱 / 客户CRM / 项目台账 / 内容数据复盘）
   - 预置示例数据
   - 生成工作台页面并部署为**在线链接**
5. 把链接发到手机，浏览器打开 → 添加到主屏幕。所有设备数据自动同步
6. 之后想改功能（加字段、加模块），直接在对话里用自然语言说，AI 会迭代升级且不丢数据

---

## 方式三：Codex / Claude Code 等本地编程 AI

适合希望在本地跑、并让 AI 帮忙定制的情况。

**最快用法（不需要任何命令）：**
- AI 下载本仓库后，直接用浏览器打开 `workspace.html` 即可

**让 AI 帮你起一个本地服务（可选）：**
把下面的内容发给你的 AI：

```
请把这个项目在本地跑起来：
1. 进入项目目录
2. 启动一个静态文件服务器：python -m http.server 8080
3. 告诉我访问地址 http://localhost:8080/workspace.html
4. 之后我要改字段/加模块时，直接修改 workspace.html 里的 MODULES 配置数组
```

**注意事项：**
- `localhost` 服务只能本机访问；如果要给别人看，用方式二
- 本地模式的跨浏览器数据迁移同「方式一」的数据说明

---

## 方式四：豆包 / Manus / 其他云端 AI

这些平台没有持久数据表能力或能力形态不同，有两种玩法：

**A. 让 AI 按规格重建（推荐）**
把 [`prompts/any-ai-prompt.md`](prompts/any-ai-prompt.md) 全文复制发给 AI。
这份 Prompt 写清了完整的产品规格：8 个模块、全部字段、页面结构、设计规范、
单文件 + localStorage 存储等硬约束。AI 会生成一份你可以直接保存使用的 HTML。

**B. 定制你自己的版本**
在 A 的基础上继续追加需求，例如：
- 「把客户合作阶段改成我的行业话术」
- 「加一个『拍摄档期』模块，字段：日期、时段、账号、拍摄内容、地点」
- 「把主题色从橙色改成蓝色」

生成后保存为 `.html` 文件，双击打开即用（同方式一）。

---

## 常见问题

**Q：数据会丢吗？**
本地模式：正常使用不会丢。清浏览器缓存/换设备前请先「导出备份」。
平台模式（方式二）：存在云端数据表中，自动同步。

**Q：手机和电脑数据能同步吗？**
本地模式不能（各自浏览器独立存储）。用方式二部署即可多端同步。

**Q：怎么改字段、加模块？**
- 会改代码：打开 `workspace.html`，找到 `MODULES` 数组，照着现有格式改即可，渲染/表单/筛选全自动跟随
- 不会改代码：把需求发给任何 AI（配合 `prompts/any-ai-prompt.md`），让它帮你改

**Q：可以多人一起用吗？**
不推荐。这是为"一个人操盘"设计的单人系统（数据在个人浏览器/个人账号下）。

**Q：示例数据怎么删？**
右上角「清空示例」→ 二次确认，所有带【示例】标记的数据会被删除。
